# SOP: Mempalace Execution

**Skill Reference:** `2_KNOWLEDGE/frameworks/core_system/mempalace/`
**Trigger:** Triggered by the `Memory Encoding Protocol (MEP)`.

## 1. PREPARATION
- Identify the content to be stored.
- Determine the absolute path: `3_MEMORY/specs/`, `3_MEMORY/logs/`, or `3_MEMORY/errors/`.

## 2. FORMATTING RULES
- **Headers:** Use `##` for sections and `###` for dates/milestones.
- **Content:** Do NOT paraphrase technical rules. Quote them verbatim.
- **Lists:** Use `-` for bullets.

## 3. CATEGORY CHECKLIST
- `[ ]` **Wings (Specs):** Does the filename end in `_spec.md` or `_rules.md`? Are all Hex colors and typography sizes explicitly written?
- `[ ]` **Rooms (Logs):** Does the filename track the project phase (e.g., `phase_1_log.md`)? Is there a timestamp for the entry?
- `[ ]` **Drawers (Errors):** Did you include the stack trace and the solution that fixed it?

## 4. FALSIfIABILITY CHECK
- Read back the stored memory. Could another Agent read this 2 weeks from now and perfectly reconstruct the design/code state? If no, add more specific details.
