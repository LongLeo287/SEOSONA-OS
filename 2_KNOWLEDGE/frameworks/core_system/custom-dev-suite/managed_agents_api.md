# Managed Agents API

Anthropic Managed Agents API schemas and HTTP client protocols.

## 1. Managed Sessions
Understand Managed Agent session states, SSE streams, paginated event indexes, and custom tool callbacks.

## 2. cURL standard
Maintain clean cURL headers: `Content-Type`, `x-api-key`, `anthropic-version`, `anthropic-beta: managed-agents-2026-04-01`.
