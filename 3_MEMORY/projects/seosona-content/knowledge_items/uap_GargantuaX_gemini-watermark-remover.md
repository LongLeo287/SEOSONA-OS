# KI: GargantuaX/gemini-watermark-remover

## Overview
This repository contains a tool designed to automatically remove watermarks from images generated by Google Gemini AI. The core functionality involves analyzing image data, identifying watermark regions, and applying reverse alpha blending techniques to reconstruct the original image content without the watermark.  The project includes browser extensions and userscript capabilities for practical application.

## Tech Stack (from code)
- **JavaScript/TypeScript:** The codebase is primarily written in JavaScript with TypeScript annotations (`.ts` files). This is evident from numerous `.ts` files like `src\core\adaptiveDetector.js` and the presence of type definition files such as `src/sdk/index.d.ts`.
- **Node.js:**  The project uses Node.js for its build system, evidenced by the `package.json` file which defines scripts like "dev" and "build" that execute Node commands (`node build.js`).
- **pnpm:** The package manager used is pnpm, as indicated by the presence of `pnpm-lock.yaml` and `pnpm-workspace.yaml`.
- **ONNX Runtime:**  The project utilizes ONNX Runtime for model inference, specifically a WebAssembly (WASM) build (`src/onnxruntime/ort-wasm-simd-threaded.wasm`). This is confirmed by the presence of files like `src\core\allenkFdncnnOnnxRuntime.js` and the directory structure `public/models/allenk-fdncnn`.

## Public API / Exports
Based on the `package.json` file's "exports" section, the following are exposed:

- **`./`**:  Exports types and modules for general use (e.g., `./src/sdk/index.js`).
- **`./browser`**: Browser-specific functionality (`./src/sdk/browser.js`).
- **`./image-data`**: Modules related to image data processing (`./src/sdk/image-data.js`).
- **`./node`**: Node.js specific modules (`./src/sdk/node.js`).
- **`./video`**: Video processing functionality (`./src/sdk/video.js`).
- **`./runtime-browser`**: Browser runtime components (`./src/runtime/browser.js`).
- **`./runtime-userscript`**: Userscript runtime components (`./src/runtime/userscript.js`).

## Dependencies
Based on the `package.json` file:

- `@pilio/gemini-watermark-tool`:  Likely a core dependency for watermark detection and removal algorithms (referenced in `src\core\allenkFdncnnDenoise.js`).
- `onnxruntime-web`: Used for running ONNX models in the browser (`import * as wasmOrt from 'onnxruntime-web/wasm';` in `src\core\allenkFdncnnOnnxRuntime.js`).

## Architecture Patterns
- **Pipeline Pattern:** The code demonstrates a pipeline architecture, particularly within files like `src\core\imageWatermarkPipeline.js`.  This involves breaking down the watermark removal process into distinct stages (initial selection, alpha repair, finalization) and passing data between them.
- **Modular Design:** The project is structured with numerous modules (`src/core/*`, `src/sdk/*`) each responsible for a specific task, promoting code reusability and maintainability.
- **Configuration-Driven:**  The watermark removal process appears to be highly configurable, with parameters like alpha gain, logo size, and margin sizes being adjustable through options objects (e.g., in `src\core\blendModes.js`).



## Relevance to SEOSONA OS
This project's code could benefit SEOSONA OS in the following ways:

- **Image Processing Capabilities:** The core watermark removal algorithms and image processing techniques could be integrated into SEOSONA OS for general image cleanup or content modification tasks.
- **AI Integration:**  The use of ONNX Runtime demonstrates a capability to integrate AI models, which aligns with SEOSONA OS's potential focus on AI-powered features.
- **Browser Extension Development:** The existing browser extension infrastructure could be leveraged to create SEOSONA OS extensions for image manipulation or content filtering within web browsers.


## UAP Routing (auto-classified)
- **System:** `seosona-content` · **Function:** `seo-metadata` · **Fit:** 66/100 · **Auto-apply:** True
- **Evidence:** `metadata`, `thumbnail`
- **All scores:** {'seosona-os': 41, 'seosona-video': 22, 'seosona-content': 66, 'seosona-ux-ui': 33, 'seosona-flow': 28}
