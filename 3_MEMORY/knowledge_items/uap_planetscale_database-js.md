# KI: planetscale/database-js

## Overview
A Fetch API-compatible PlanetScale database driver

## Architecture & Tech Stack
- Node.js / TypeScript / JavaScript
- **Total files:** 29 files across 6 directories
- **File types:** .ts: 10, .yml: 6, .json: 5, .md: 2, .gitignore: 1, .npmrc: 1, .json5: 1
- **Dev dependencies:** @types/node, @types/sqlstring, @typescript-eslint/eslint-plugin, @typescript-eslint/parser, eslint, eslint-config-prettier, eslint-plugin-prettier, prettier
- **Keywords:** planetscale, database, mysql, vitess, serverless, vercel, lambda

## Documentation Sections
- PlanetScale serverless JavaScript driver for Vitess/MySQL
- Installation
- Usage
- Database URL
- Connection factory
- Transactions
- Custom fetch function
- Custom query parameter format function
- Custom type casting function
- Row return values
- Development
- Need help?
- License

## Available Commands
- `npm run clean` -- rm -rf dist/
- `npm run build` -- npm run clean && tsc && tsc --module commonjs --outDir dist/cjs && echo '{"type"
- `npm run lint` -- eslint src/ __tests__/
- `npm run test` -- npm run build && node --import tsx --test __tests__/*.test.ts
- `npm run version` -- echo "export const Version = '$npm_package_version'" > src/version.ts && git add

## Core Structure
```
  .gitignore
  .npmrc
  CODEOWNERS
  LICENSE
  README.md
  package-lock.json
  package.json
  renovate.json5
  tsconfig.json
  .github/
    workflows/
      actions-security.yml
      ci.yml
      codeql.yml
      licensing.yml
      publish.yml
  __tests__/
    cast.test.ts
    golden.test.ts
    index.test.ts
    sanitization.test.ts
    text.test.ts
  doc/
    dependency_decisions.yml
  golden/
    README.md
    cli.txt
    database.json
    dual.json
    generate.sh
    test.sql
  src/
    cast.ts
    index.ts
    sanitization.ts
    text.ts
    version.ts
```

## Quick Start
```bash
npm install @planetscale/database
A single database URL value can be used to configure the `host`, `username`, and `password` values.
Use the `Client` connection factory class to create fresh connections for each transaction or web request handler.
Use the `transaction` function to safely perform database transactions. If any unhandled errors are thrown during execution of the transaction, the transaction will be rolled back.
The following example is based on [the Slotted Counter Pattern](https://planetscale.com/blog/the-slotted-counter-pattern).
Node.js version 18 includes a built-in global `fetch` function. When using an older version of Node.js, you can provide a custom fetch function implementation. We recommend the [`undici`][1] package on which Node's built-in fetch is based.
[1]: https://github.com/nodejs/undici
```

## Analysis Note
> This KI was generated by **enhanced local structural analysis** (no LLM API was available at generation time). It includes full tech stack detection, README parsing, dependency analysis, and feature extraction. For deeper semantic analysis, re-run with an active Gemini or OpenAI API key.
