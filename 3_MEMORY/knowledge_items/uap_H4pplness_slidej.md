# KI: H4pplness/slidej

## Overview
Generate PPTX from JSON. Parse PPTX back to JSON.

## Architecture & Tech Stack
- Node.js / TypeScript / JavaScript
- **Total files:** 32 files across 6 directories
- **File types:** .js: 14, .json: 11, .md: 5, .gitignore: 1, .png: 1
- **Key dependencies:** chalk, commander, fast-xml-parser, jszip
- **Keywords:** pptx, powerpoint, slides, animation, cli, json

## Documentation Sections
- SlideJ
- Installation
- Quick Start
- CLI Commands
- Options
- Templates
- Template workflow
- 4. Edit deck.json — change text, colors, add/remove slides
- Save your own
- JSON Format
- Top-level structure
- Slide
- Elements
- Animations
- Library API
- For AI Agents
- Project Structure
- License

## Available Commands
- `npm run generate` -- node src/cli.js generate
- `npm run parse` -- node src/cli.js parse
- `npm run test` -- node src/cli.js example -o /tmp/test.json && node src/cli.js generate /tmp/test.

## Core Structure
```
  .gitignore
  DESIGN.md
  README-vi.md
  README.md
  banner.png
  package-lock.json
  package.json
  skills/
    SKILL.md
  src/
    SCHEMA.md
    cli.js
    index.js
    generators/
      animation.js
      components.js
      content-types.js
      index.js
      presentation.js
      rels.js
      slide-master.js
      slide.js
      theme.js
    parsers/
      index.js
    utils/
      constants.js
      xml-helpers.js
  templates/
    candy-pop.json
    dark-modern.json
    minimal.json
    neon-garden.json
    ocean-aurora.json
    pitch-deck.json
    report.json
    sunset-wave.json
    title-slide.json
```

## Quick Start
```bash
npm install -g slidej          # CLI tool
npx skills add H4pplness/slidej  # AI agent local skill
slidej example -o demo.json          # 1. Generate sample JSON
slidej generate demo.json -o demo.pptx  # 2. Build the PPTX
slidej parse demo.pptx -o parsed.json   # 3. Parse it back
slidej generate parsed.json -o edit.pptx # 4. Edit & regenerate
slidej generate input.json -o output.pptx           # Generate a PPTX
slidej parse input.pptx -o output.json               # Parse PPTX to file
slidej parse input.pptx                              # Parse to stdout (for piping)
slidej parse input.pptx --no-images -o output.json   # Skip embedded images
```

## Analysis Note
> This KI was generated by **enhanced local structural analysis** (no LLM API was available at generation time). It includes full tech stack detection, README parsing, dependency analysis, and feature extraction. For deeper semantic analysis, re-run with an active Gemini or OpenAI API key.
