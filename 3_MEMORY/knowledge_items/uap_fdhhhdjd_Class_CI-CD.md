# KI: fdhhhdjd/Class_CI-CD

## Overview
> Khóa học thực hành xây dựng pipeline CI/CD hoàn chỉnh từ đầu — > từ tự động hóa deploy bằng PM2 đến containerize với Docker và nhận thông báo qua Telegram.

## Architecture & Tech Stack
- Node.js / TypeScript / JavaScript
- **Frameworks:** Express.js
- **Total files:** 26 files across 5 directories
- **File types:** .png: 12, .yml: 5, .md: 3, .json: 2, .gitignore: 1, .js: 1, .sh: 1
- **Key dependencies:** dotenv, express

## Documentation Sections
- 🤖 Class CI/CD — PM2 · Docker · GitHub Actions · VPS
- 📋 Mục lục
- 🎯 Giới thiệu
- 🏗 Kiến trúc Pipeline
- 1. CI/CD với PM2
- 2. CI/CD với Docker
- 3. CI/CD với Docker + Telegram Notification
- 🛠 Tech Stack
- 📁 Cấu trúc dự án
- ✅ Yêu cầu
- 🚀 Hướng dẫn cài đặt
- 1. Clone repository
- 2. Cài đặt dependencies
- 3. Tạo file `.env`
- 4. Chạy với PM2 (development)
- 5. Thiết lập Self-hosted Runner trên VPS
- Tạo thư mục runner
- Cấu hình runner (lấy token từ repo Settings > Actions > Runners)
- Chạy runner như service
- 6. Cấu hình GitHub Secrets
- 🐳 Chạy với Docker
- Build và start container
- Dừng container
- 📡 API Endpoint
- 📞 Liên hệ

## Available Commands
- `npm run dev` -- node --watch server.js
- `npm run test` -- echo "Error: no test specified" && exit 1

## Core Structure
```
  .gitignore
  CI-CD.md
  DIAGRAM.md
  Dockerfile
  README.md
  docker-compose.yml
  makefile
  package-lock.json
  package.json
  server.js
  .github/
    FUNDING.yml
    scripts/
      custom.sh
    workflows/
      cicd-workflow.yml
  actions/
    cicd-best.yaml
    cicd-workflow.yml
    node.js.yml
  assets/
    CI-1.png
    CI.png
    diagram.png
    diagram1.png
    diagram3.png
    env.png
    env1.png
    label.png
    label1.png
    setting.png
    setting1.png
    ssh-keys.png
```

## Quick Start
```bash
Push Code → GitHub Actions → SSH vào VPS → Pull Code → PM2 Restart
Push Code → GitHub Actions → Build Docker Image → Push Registry → VPS Pull & Run
Push Code → GitHub Actions → Build & Deploy Docker → Telegram Bot thông báo kết quả
Class_CI-CD/
├── .github/
│   ├── workflows/
│   │   └── cicd-workflow.yml     # GitHub Actions workflow
│   └── scripts/                  # Helper scripts
├── assets/
│   ├── diagram.png               # Sơ đồ CI/CD với Docker
```

## Analysis Note
> This KI was generated by **enhanced local structural analysis** (no LLM API was available at generation time). It includes full tech stack detection, README parsing, dependency analysis, and feature extraction. For deeper semantic analysis, re-run with an active Gemini or OpenAI API key.
