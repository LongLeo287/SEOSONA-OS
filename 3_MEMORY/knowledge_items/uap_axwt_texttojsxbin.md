# KI: axwt/texttojsxbin

## Overview
Encode selected text to JSXBIN and put inside eval

## Architecture & Tech Stack
- Node.js / TypeScript / JavaScript
- **Total files:** 14 files across 3 directories
- **File types:** .json: 8, .gitignore: 1, .vscodeignore: 1, .md: 1, .gif: 1, .png: 1, .ts: 1
- **Dev dependencies:** @types/mocha, @types/node, tslint, typescript, vscode
- **Keywords:** ExtendScript, JSXBIN, JSX, After Effects

## Documentation Sections
- TextToJSXBIN
- Installation
- In action
- Troubleshooting
- Known issues

## Available Commands
- `npm run vscode:prepublish` -- npm run compile
- `npm run compile` -- tsc -p ./
- `npm run watch` -- tsc -watch -p ./
- `npm run postinstall` -- node ./node_modules/vscode/bin/install
- `npm run test` -- npm run compile && node ./node_modules/vscode/bin/test

## Core Structure
```
  .gitignore
  .vscodeignore
  README.md
  package-lock.json
  package.json
  tsconfig.json
  tslint.json
  .vscode/
    extensions.json
    launch.json
    settings.json
    tasks.json
  images/
    demo.gif
    icon.png
  src/
    extension.ts
```

## Analysis Note
> This KI was generated by **enhanced local structural analysis** (no LLM API was available at generation time). It includes full tech stack detection, README parsing, dependency analysis, and feature extraction. For deeper semantic analysis, re-run with an active Gemini or OpenAI API key.
