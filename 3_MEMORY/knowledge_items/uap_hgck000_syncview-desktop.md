# KI: hgck000/syncview-desktop

## Overview
A lightweight desktop application to compare 1–4 images and synchronize operations.

## Architecture & Tech Stack
- Node.js / TypeScript / JavaScript
- **Total files:** 63 files across 15 directories
- **File types:** .tsx: 13, .ts: 12, .json: 7, .py: 5, .spec: 3, .gitignore: 2, .md: 2
- **Key dependencies:** @tailwindcss/vite, tinykeys
- **Dev dependencies:** autoprefixer, postcss, tailwindcss

## Core Capabilities
- Drag and drop; auto layout: `1–3 images → 3 columns`, `4 images → 2×2`.
- Smooth pan/zoom; zoom around cursor; Double-click: Fit ↔ 2×.
- Link-All: synchronize pan/zoom of all panes.
- Loupe: change zoom and size instantly.
- Display detailed EXIF: camera information, lens, ISO, aperture, shutter, date, location (if available).
- Vertical tabs: New / Rename / Close; Autosave Last Session (restore session).
- Export `.exe` installation file for end users, no need to install Python or Node.
- Basic Hotkeys + Help overlay.

---

## Documentation Sections
- SyncView (PyWebview + FastAPI + React + Tailwind 4)
- Features
- Tech
- Backend
- Frontend
- Preview
- Quick Start (after clone)
- ..FOR DEV..
- Windows
- Ubuntu
- RUN
- Terminal 1 - Backend
- Terminal 2 - Frontend
- DEV APP (PyWebview host)
- Windows
- Ubuntu
- ..FOR USER..

## Core Structure
```
  .editorconfig
  .gitignore
  README.md
  SyncView.spec
  installer.iss
  package-lock.json
  package.json
  .github/
    workflows/
      release.yml
  Output/
    SyncView-0.0.0-Setup.exe
  assets/
    MicrosoftEdgeWebview2Setup.exe
    SyncView.ico
    demo1.gif
    demo2.gif
    syncview-icon.png
  backend/
    SyncView.spec
    requirements-dev.txt
    requirements.txt
    run_dev.py
    run_prod.py
    run_prod.spec
    app/
      __init__.py
      bridge.py
      main.py
  frontend/
    .gitignore
    README.md
    eslint.config.js
    index.html
    package-lock.json
    package.json
    tailwind.config.ts
    tsconfig.app.json
    tsconfig.json
    tsconfig.node.json
    vite.config.ts
    public/
      SyncView.ico
      syncview-qr.png
      vite.svg
    src/
      App.css
      App.tsx
      index.css
      main.tsx
      app/
        annotCoords.ts
        bridge.ts
        cursorManager.ts
        hotkeys.tsx
        init.ts
        path.ts
        store.ts
        useAnnotCanvas.ts
        useImageCanvas.ts
      assets/
        react.svg
      components/
        DropZone.tsx
        HelpOverlay.tsx
        Pane.tsx
        Sidebar.tsx
        TabsBar.tsx
        TextLayer.tsx
        Toolbar.tsx
        ViewerGrid.tsx
      dev/
        AppEventDebug.tsx
        DndProbe.tsx
      lib/
        tinykeys-compat.ts
      types/
        tinykeys.d.ts
```

## Quick Start
```bash
cd frontend
npm ci
npm run dev
python -m venv .venv
.\.venv\Scripts\pip install --upgrade pip
.\.venv\Scripts\pip install -r backend\requirements.txt
python3 -m venv .venv
source .venv/bin/activate
pip install --upgrade pip
pip install -r backend/requirements.txt
```

## Analysis Note
> This KI was generated by **enhanced local structural analysis** (no LLM API was available at generation time). It includes full tech stack detection, README parsing, dependency analysis, and feature extraction. For deeper semantic analysis, re-run with an active Gemini or OpenAI API key.
