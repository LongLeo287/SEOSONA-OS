# KI: tranhaonguyendev/Lazy-downloader

## Overview
All-in-one media downloader CLI (Node.js)

## Architecture & Tech Stack
- Node.js / TypeScript / JavaScript
- **Frameworks:** Playwright
- **Total files:** 20 files across 2 directories
- **File types:** .ts: 16, .json: 2, .gitignore: 1, .md: 1
- **Key dependencies:** playwright
- **Dev dependencies:** @types/node, typescript

## Documentation Sections
- Dự án lazy Downloader 1.0.0
- Install
- CLI
- Options (tóm tắt)
- Upload Mode
- Only URL Mode
- Auth Note
- Library (Node.js)
- Progress Hooks
- Docs
- License

## Available Commands
- `npm run build` -- tsc -p tsconfig.json
- `npm run start` -- node dist/cli.js
- `npm run test:help` -- node dist/cli.js --help
- `npm run prepack` -- npm run build

## Core Structure
```
  .gitignore
  LICENSE
  README.md
  package.json
  tsconfig.json
  src/
    auth-cache.ts
    cli.ts
    endpoints.ts
    index.ts
    progress.ts
    template-engine.ts
    worker.ts
    worker/
      auth.ts
      challenge-bridge.ts
      content.ts
      fs-utils.ts
      job.ts
      meta.ts
      options.ts
      pow.ts
      transfer.ts
```

## Quick Start
```bash
npm install -g lazy-downloader
npx playwright install chromium
lazy-down "URL" -P downloads --all -o "%(title)s_%(idx)s.%(ext)s"
lazy-down "URL" \
--upload-url "http://localhost:8787/upload" \
--upload-field "file" \
--remove-local-after-upload
lazy-down "URL" --only-url --all
Hook nhận object trạng thái.
```

## Analysis Note
> This KI was generated by **enhanced local structural analysis** (no LLM API was available at generation time). It includes full tech stack detection, README parsing, dependency analysis, and feature extraction. For deeper semantic analysis, re-run with an active Gemini or OpenAI API key.
