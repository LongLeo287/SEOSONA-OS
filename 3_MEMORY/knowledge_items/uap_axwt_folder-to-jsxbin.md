# KI: axwt/folder-to-jsxbin

## Overview
1. It will first copy selected folder with all it's content and add the ending `_encoded` in the folder name 2. Recursivelt encode all JSX files to JSXBIN files and remove the original JSX files withn `_encoded` folder 3. Display encoding progress via Apple Script at the top right corner 4. Any issues that may occur should be available in the `~/Desktop/JSXBIN_errors.log` file

## Architecture & Tech Stack
- Could not detect automatically
- **Total files:** 7 files across 5 directories
- **File types:** .png: 2, .DS_Store: 1, .md: 1, .plist: 1, .wflow: 1, .sh: 1

## Documentation Sections
- This Quick Action enables batch conversion of the JSX to JSXBIN files withn selected folder
- Description:
- How to use:
- Install NVM and Node.js v14.21.3
- Install NVM
- Install Node.js v14.21.3
- Set Default Node.js Version
- Switch Between Node.js Versions

## Core Structure
```
  .DS_Store
  README.md
  Encode JSX to JSXBIN.workflow/
    Contents/
      Info.plist
      document.wflow
      QuickLook/
        Preview.png
        Thumbnail.png
      _CodeSignature/
        CodeDirectory
        CodeRequirements
        CodeRequirements-1
        CodeResources
        CodeSignature
  src/
    encode.sh
```

## Quick Start
```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.38.0/install.sh | bash
nvm --version
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
nvm install 14.21.3
node --version
```

## Analysis Note
> This KI was generated by **enhanced local structural analysis** (no LLM API was available at generation time). It includes full tech stack detection, README parsing, dependency analysis, and feature extraction. For deeper semantic analysis, re-run with an active Gemini or OpenAI API key.
