# KI: agrinman/tunnelto

## Overview
`tunnelto` lets you expose your locally running web server via a public URL. Written in Rust. Built completely with async-io on top of tokio.

## Architecture & Tech Stack
- Rust
- **Total files:** 48 files across 16 directories
- **File types:** .rs: 23, .toml: 5, .html: 5, .yml: 3, .json: 2, .dockerignore: 1, .gitattributes: 1

## Documentation Sections
- `tunnelto`
- Install
- Brew (macOS)
- Cargo
- Everywhere
- Usage
- Quick Start
- More Options:
- Host it yourself
- Testing Locally
- Run the Server: xpects TCP traffic on 8080 and control websockets on 5000
- Run a local tunnelto client talking to your local tunnelto_server
- Test it out!
- Remember 8080 is our local tunnelto TCP server
- Caveats for hosting it yourself

## Core Structure
```
  .dockerignore
  .gitattributes
  .gitignore
  Cargo.lock
  Cargo.toml
  Dockerfile
  LICENSE
  Procfile
  README.md
  fly.toml
  musl_build.sh
  .github/
    workflows/
      check.yml
      publish_crate.yml
      release.yml
  .vscode/
    launch.json
    tasks.json
  tunnelto/
    Cargo.toml
    src/
      cli_ui.rs
      config.rs
      error.rs
      local.rs
      main.rs
      update.rs
      introspect/
        console_log.rs
        mod.rs
    static/
      css/
        styles.css
        styles.css.map
      img/
        logo.png
        wormhole_ascii.txt
    templates/
      base.html
      body_detail.html
      detail.html
      headers_detail.html
      index.html
  tunnelto_lib/
    Cargo.toml
    src/
      lib.rs
  tunnelto_server/
    Cargo.toml
    src/
      active_stream.rs
      config.rs
      connected_clients.rs
      control_server.rs
      main.rs
      observability.rs
      remote.rs
      auth/
        auth_db.rs
        client_auth.rs
        mod.rs
        reconnect_token.rs
      network/
        mod.rs
        proxy.rs
        server.rs
```

## Quick Start
```bash
brew install agrinman/tap/tunnelto
cargo install tunnelto
The above command opens a tunnel and forwards traffic to `localhost:8000`.
1. Compile the server for the musl target. See the `musl_build.sh` for a way to do this trivially with Docker!
2. See `Dockerfile` for a simple alpine based image that runs that server binary.
3. Deploy the image where ever you want.
```

## Analysis Note
> This KI was generated by **enhanced local structural analysis** (no LLM API was available at generation time). It includes full tech stack detection, README parsing, dependency analysis, and feature extraction. For deeper semantic analysis, re-run with an active Gemini or OpenAI API key.
