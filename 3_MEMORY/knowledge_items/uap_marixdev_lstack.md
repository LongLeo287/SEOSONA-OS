# KI: marixdev/lstack

## Overview
Local development environment for Windows and Linux

## Architecture & Tech Stack
- Node.js / TypeScript / JavaScript
- **Frameworks:** React
- **Total files:** 72 files across 16 directories
- **File types:** .ts: 20, .tsx: 17, .svg: 8, .md: 5, .json: 5, .png: 3, .cs: 3
- **Key dependencies:** @xterm/addon-fit, @xterm/xterm, axios, chokidar, electron-updater, extract-zip, fs-extra, lucide-react, node-pty, react, react-dom, react-router-dom
- **Dev dependencies:** @types/fs-extra, @types/node, @types/react, @types/react-dom, @vitejs/plugin-react, autoprefixer, electron, electron-builder
- **Keywords:** lstack, local-dev, electron, php, nginx

## Core Capabilities
- **One-click service management** — start, stop, and restart every service from the dashboard
- **Virtual hosts** — auto-create `.test` domains (e.g. `myapp.test`) with Nginx or Apache
- **SSL certificates** — trust a local CA with a single click; full HTTPS on every project
- **Per-project PHP profiles** — assign a dedicated PHP-FPM process with isolated `php.ini` settings and extensions per project
- **PHP extension control** — enable/disable extensions per profile; built-in profiles ship pre-configured for Laravel, WordPress, Symfony, and more
- **Project wizard** — scaffold new projects from a template with one click; installs CMS/framework files automatically
- **Framework detection** — automatically detects Laravel, WordPress, Symfony, and CodeIgniter in existing folders
- **phpMyAdmin** — accessible at `phpmyadmin.test` with auto-login (no password required for root)
- **Built-in terminal** — xterm.js-powered full terminal per project, opens directly in the project directory
- **Real-time logs** — tail live service and access logs from the UI
- **Package manager** — download and install service binaries (Nginx, Apache, MariaDB, PHP, Redis, etc.) directly from the app, no system-wide installs needed
- **Auto-updater** — built-in update checker via electron-updater
- **Bilingual UI** — full English and Vietnamese interface, switchable at runtime

---

## Documentation Sections
- Overview
- Features
- Services
- PHP Profiles
- Project Templates
- Special Domains
- Installation
- Pre-built binaries
- Building from Source
- Prerequisites
- Steps
- Windows
- Linux (run on Linux or WSL)
- Data Directory
- Settings
- License

## Available Commands
- `npm run dev` -- vite
- `npm run start` -- vite
- `npm run build` -- vite build
- `npm run preview` -- vite preview
- `npm run dist` -- vite build && electron-builder
- `npm run dist:win` -- vite build && electron-builder --win
- `npm run dist:linux` -- vite build && electron-builder --linux
- `npm run lint` -- eslint --ext .ts,.tsx src electron

## Core Structure
```
  .gitignore
  CHANGELOG.md
  CHANGELOG.vi.md
  LICENSE
  README.md
  README.vi.md
  electron-builder.json
  icon.png
  index.html
  logo.svg
  package-lock.json
  package-registry.json
  package.json
  postcss.config.js
  tailwind.config.js
  tsconfig.json
  vite.config.ts
  .github/
    copilot-instructions.md
    workflows/
      release.yml
  main/
    main.ts
    preload.ts
    core/
      CertManager.ts
      PackageManager.ts
      PhpFpmManager.ts
      PhpProfileManager.ts
      ServiceManager.ts
      TerminalManager.ts
      VHostManager.ts
    ipc/
      index.ts
      nginxMenu.ts
      phpMenu.ts
      serviceMenus.ts
  public/
    icon.png
    icons/
      codeigniter.svg
      drupal.svg
      joomla.svg
      laravel.svg
      prestashop.svg
      symfony.svg
      wordpress.svg
  src/
    App.tsx
    index.css
    main.tsx
    components/
      AboutModal.tsx
      InstallTerminal.tsx
      LogModal.tsx
      LogViewer.tsx
      ServiceCard.tsx
      Sidebar.tsx
      TerminalPanel.tsx
      TerminalView.tsx
      TitleBar.tsx
      Toast.tsx
    i18n/
      index.ts
      locales/
        en.ts
        vi.ts
    pages/
      Packages.tsx
      PhpProfiles.tsx
      PortManager.tsx
      Projects.tsx
      Settings.tsx
    store/
      index.ts
    types/
      global.d.ts
      index.ts
  win-setup/
    App.xaml
    App.xaml.cs
    AssemblyInfo.cs
    LStackInstaller.csproj
    MainWindow.xaml
    MainWindow.xaml.cs
    app.manifest
    icon.ico
    Assets/
      icon.png
```

## Quick Start
```bash
chmod +x LStack-1.0.0.AppImage
./LStack-1.0.0.AppImage
sudo dpkg -i lstack_1.0.0_amd64.deb
lstack
sudo rpm -i lstack-1.0.0.x86_64.rpm
lstack
git clone https://github.com/marixdev/lstack.git
cd lstack
npm install
npm run dev
```

## Analysis Note
> This KI was generated by **enhanced local structural analysis** (no LLM API was available at generation time). It includes full tech stack detection, README parsing, dependency analysis, and feature extraction. For deeper semantic analysis, re-run with an active Gemini or OpenAI API key.
