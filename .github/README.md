# .github — GitHub configuration

CI, community health files, and repo assets.

| Item | Purpose |
|---|---|
| `workflows/` | GitHub Actions — CI (integrity + checks) and a CodeQL security-scan gate. |
| `assets/` | README/branding images (logo, banners). |
