# SEOSONA OS — Full Audit (2026-07-24)

**Method:** 5 parallel read-only agents over the OS's own code (85 Python + 48 JS in 1_CORE, cli,
config) — Python core · JS/hooks/cli/bridge · config/structure · security · tests/CI/docs. The ~8,000
KI/vendored-skill files were treated as data/third-party, not audited line-by-line.

**Verdict: B+ — healthy core, no critical security exposure; a few real bugs (now fixed) and standing
hardening/test gaps.** Runtime verified: `npm run lint` PASS, capability-bridge `validate` ok (1,672
resources resolve, 283 skills), `npm test` 52 assertions pass, all Python compiles, no broken doc links.

---

## Issues & status

### HIGH
| # | Issue | Status |
|---|---|---|
| H1 | **CLI totally broken** — `cli/src/localInit.js:165` had a duplicated paste leaving functions unclosed; `seosona.js` requires it at load, so *every* `seosona` command crashed. | ✅ **FIXED** (removed the dup block; `node --check` passes) |
| H2 | **Clone couldn't fetch the code-nav binary** — `1_CORE/bin/` was fully gitignored, so `install.ps1` (the README's install step) was absent on a fresh clone. | ✅ **FIXED** (gitignore now excludes only `1_CORE/bin/**/*.exe`; `install.ps1`+LICENSE+NOTICES tracked) |
| H3 | **UAP queue corruption** — `uap_manager.py:70` marked *every* in-flight repo FAILED on any one repo's error (no `id` filter), losing other repos' work in a large run. | ✅ **FIXED** (marks only the single oldest in-flight row) |
| H4 | **OS Python is 100% untested** — 85 `.py` files (UAP pipeline, security guard, dispatcher, connectors) have zero automated tests; the only tests cover a small `hooks/lib` slice. | ⚠️ **OPEN** — recommend pytest smoke tests for `02b_security_guard.py` + dispatcher guard, wired into `prepublishOnly`. |

### MEDIUM
| # | Issue | Status |
|---|---|---|
| M1 | **Integrity guard failed OPEN** — `git()` swallowed errors → empty deletion list → mass-delete guard passed. | ✅ **FIXED** (returns null on git failure → guard blocks fail-closed) |
| M2 | **Dispatcher side-effect guard porous** — the denylist missed `remove/purge/revoke/post/transfer/grant/…` so such scripts could auto-run. | ✅ **FIXED** (denylist expanded; verified blocks the missing verbs, still allows audit/analyzer) |
| M3 | **`setup-hooks.js` clobbers existing hooks** — unconditionally overwrites `.git/hooks/pre-commit`/`pre-push`, destroying husky/other hooks. | ⚠️ **OPEN** — detect + chain instead of overwrite. |
| M4 | **`seosona setup` never creates the `~/.seosona` junction** — yet `doctor`/`init` require it. | ⚠️ **OPEN** — create the link in `runSetup` (or a PS installer) + handle the connector throw. |
| M5 | **`_extract_domain` false-matches version numbers** — "review the 2.0 spec" → dispatch `--domain 2.0`. | ⚠️ **OPEN** — require a real TLD. |
| M6 | **Assimilator DB leak** — `03_assimilator.py` opens sqlite without try/finally; a mid-loop raise leaks the connection + strands the item at AUDITED. | ⚠️ **OPEN** — try/finally + per-item try/except-continue. |
| M7 | **SSRF: TOCTOU + redirects** — `url_guard.assert_safe_url` re-resolves DNS at fetch (rebinding), follows 3xx to private hosts; 3 connectors skip the guard. | ⚠️ **OPEN** — pin the validated IP, disable auto-redirect + re-validate each hop, add the guard to the 3 connectors. |
| M8 | **Version drift** — `package.json` 9.0.0 vs `system_settings.yaml` os_version 6.0. | ✅ **FIXED** (os_version → 9.0) |
| M9 | **`docs/00_master_architecture.md` UAP section stale** — describes an old Excel-driven 6-agent model; reality is `uap_manager`/`02b_security_guard`/`03_assimilator`. | ⚠️ **OPEN** — rewrite 00's UAP section to match `docs/03`. |
| M10 | **SAST gap** — CodeQL was removed (it fatally errored on a vendored Ruby skill). | ⚠️ **ACCEPTED** — defensible for a vendored-heavy repo; consider Dependabot or CodeQL scoped away from `.agents/skills`+`2_KNOWLEDGE`. |

### LOW (selected)
| # | Issue | Status |
|---|---|---|
| L1 | README/SETUP said "37 vendored skills" (really 35 + 2 tier-organizer dirs). | ✅ **FIXED** (→ 35) |
| L2 | `mcp_servers.json` stale/disjoint from live `.mcp.json` (template with `YOUR_TOKEN_HERE`). | ⚠️ OPEN — rename to `*.example.json`. |
| L3 | `isSafeToAutoExecute` greenlights `sop`/`knowledge_item` regardless of risk wording. | ⚠️ OPEN — read-only docs, low risk. |
| L4 | Bare `except:` in `backlink_connector.py:113/141`; no-op regex `bridge:171`; dead var + `includes('scripts')` substring bug in `language_linter.js`; `cli/package.json main`→missing `index.js`; orphaned daemons (oa_academy/oap_intake/legacy_upgrade not in boot manager); `is_uap_relevant` returns list not bool; raw dict repr in KI markdown; weak default gateway key; stale path comments. | ⚠️ OPEN — cosmetic/low-risk, batch later. |

---

## Confirmed healthy
- **Security: no real secrets in the tree** — every `sk-`/`ghp_` hit is a test fixture or a detector regex; `.env`/credentials correctly gitignored + untracked. No `shell=True`/`eval`/`os.system`/`pickle.load` on untrusted input in own code; all `subprocess` uses argv arrays. Dispatcher repo-confinement (`relative_to(ROOT)`) is not bypassable.
- **This session's changes verified coherent**: dispatcher side-effect guard, `mcp_knowledge_server` (FastMCP), `plugin_manager` router+agent-skill scan, `vector_memory` persist/self-heal, `classifier` evidence-tiering — all correct.
- Git hooks installed correctly; `publish.yml` CI is sound (no secret leakage); all JSON/YAML parse; per-folder READMEs present; no broken markdown links.

---

## Fixed this pass (commit)
H1 CLI · H2 install.ps1 · H3 UAP queue · M1 integrity-guard fail-closed · M2 dispatcher denylist · M8 version · L1 skill count. Remaining OPEN items are hardening (SSRF, hooks, assimilator), the Python test suite (H4), and the stale `docs/00` — recommended, not blocking.
