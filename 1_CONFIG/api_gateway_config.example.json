{
  "server": {
    "host": "127.0.0.1",
    "port": 8000,
    "debug": false
  },
  "security": {
    "_comment": "REQUIRED: replace with a strong random key. Gateway refuses to start with the placeholder. Never bind host to 0.0.0.0 unless behind an authenticated proxy.",
    "api_key": "CHANGE_ME_TO_A_STRONG_RANDOM_KEY",
    "allowed_ips": ["127.0.0.1"]
  },
  "webhooks": {
    "telegram": false,
    "slack": false
  }
}
